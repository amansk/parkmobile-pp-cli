# parkmobile-pp-cli
Agent-native Printing Press CLI for ParkMobile consumer accounts. Zone lookup, sessions, hard-gated start/stop/extend via the same APIs as the ParkMobile app/web — not the partner developer portal.
